# CODEOWNERS — intentionally no owners.
#
# Nothing in this repo enforces code-owner review: no branch protection rule or
# ruleset sets `require_code_owner_reviews`. Owner lines here would gate no merge;
# they would only put every PR into JC's review queue as noise.
#
# To make a path require JC's review, add owner lines here TOGETHER WITH
# `require_code_owner_reviews: true` in the default branch's protection — never one
# without the other. Model: Direccion.Tpu.mx (.github/CODEOWNERS +
# tests/codeowners.test.ts).
#
# Decision: JC, 2026-09-24 (plan 2026-09-23-direccion-tpu-mx-costeo-bi, D12).
